# import MenuProduct from '../fragments/menu-product.graphql'

query Menu($category: Category, $search: String) {
  menu(
    filter: { category: $category, search: $search }
    pagination: { limit: 12, offset: 0 }
  ) {
    products {
      ...MenuProduct
    }
  }
}
